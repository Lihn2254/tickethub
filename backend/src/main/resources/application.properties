spring.application.name=evant
#PostgreSQL database connection settings
spring.datasource.url=jdbc:postgresql://localhost:5432/tickethub
spring.datasource.username=admin
spring.datasource.password=Eva01

#JPA/Hibernate settings
#Prevents hibernate from changing the database schemas
spring.jpa.hibernate.ddl_auto=none
spring.jpa.show-sql=true

#Prevents hibernate from changing the column names to snake_case
#spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl